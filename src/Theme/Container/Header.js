import React from 'react';
import "../Css/Header.css"

const Header = () => {
  return (
    <header className="fixed-header">
      <span className="header-title">Bet Build Fixture</span>
    </header>
  );
};

export default Header;
